# Remaining tasks

- [x] Initialize the git repo and run `openspec init`
- [x] Write the spec as an OpenSpec change proposal
- [ ] Scaffold the pnpm monorepo: Hono api, dashboard, widget and shared UI
- [ ] Port the reusable patterns from ai-frontend-advisor: handler shape, caps, eval, lazy loader
- [ ] Set up Tailwind and shadcn/ui with a prefixed, shadow-root-safe config
- [ ] Set up the Supabase project, pgvector schema and migrations
- [ ] Build upload, chunking, embedding and the indexing progress bar
- [ ] Build retrieval and model answers with numbered citations
- [ ] Build the admin dashboard: settings, preview chat, logs, ratings, gaps
- [ ] Build the embeddable widget, its script tag and the chat-first landing page
- [ ] Build anonymous sessions and per-session workspace isolation
- [ ] Write the three fictional demo doc sets and the set picker
- [ ] Seed each new session from the demo template and sweep expired ones
- [ ] Add Vercel Firewall rules, per-session caps and fictional-data labels
- [ ] Deploy to a Vercel subdomain and write the README
- [ ] Add the project to the portfolio, resumeProjects and profile README
